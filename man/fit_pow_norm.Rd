% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_pow_norm.r
\name{fit_pow_norm}
\alias{fit_pow_norm}
\title{Fit power model with with two parameters and normal error to y~x data}
\usage{
fit_pow_norm(x, y, args_optim = list())
}
\arguments{
\item{x}{numeric vector. independent variable}

\item{y}{numeric vector. dependent variable}

\item{args_optim}{Additional arguments to pass to \code{\link[stats]{optim}}}
}
\description{
Fit power model \code{\link[FishRepro]{pow}} with two parameters, where residual error follows a normal distribution.
}
\examples{
# Fit model to batch fecundity~length data
x_ob <- data_fb_L$TL
y_ob <- data_fb_L$fb
fit <- fit_pow_norm(x=x_ob,y=y_ob,args_optim = list(control=list(maxit=1000)))
x_pr <- seq(min(x_ob),max(x_ob),length=100)
y_pr <- do.call(pow,c(list(x=x_pr),as.list(fit$par)))
plot(x_ob,y_ob)
points(x_pr,y_pr,type="l",lwd=2)

}
\author{
Nikolai Klibansky
}
